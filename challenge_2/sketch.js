var targetSize = 20;
var targetSizeN = 40;

function setup() {
	// create a place to draw
	createCanvas(640, 360);
	noStroke();
	noLoop();
}

function draw() {
	// clear the background
	background(130, 130, 170);
	// draw the ellipse
	stroke(255, 255, 255);
	noFill();


}
